# Profile "integrationtest": full Spring context + real MySQL (Testcontainers or host; see integration/*.java).
spring.docker.compose.enabled=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
gemini.api.key=test-integration-dummy
